import 'package:flutter/material.dart';

class IndexHomeWidget extends StatelessWidget {
  const IndexHomeWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Index'),
      ),
    );
  }
}
